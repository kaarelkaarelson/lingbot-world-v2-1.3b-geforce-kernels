# Pinned to the measured stack (Linux x86_64, Python 3.12, CUDA 12.8).
# torch/torchvision come from the cu128 index; see setup.sh.
torch==2.8.0
torchvision==0.23.0
diffusers==0.39.0
transformers==4.51.3
tokenizers==0.21.4
accelerate==1.15.0
opencv-python==4.11.0.86
easydict==1.13
ftfy==6.3.1
imageio[ffmpeg]==2.37.4
imageio-ffmpeg
einops==0.8.2
scipy==1.17.1
tqdm
numpy<2
safetensors
torchao==0.13.0
huggingface_hub[cli]
# SageAttention 2.2 (sm_120 wheel) and flash_attn 2.8.3 (optional; cross-attention
# falls back to SDPA without it) are installed by setup.sh from the GitHub release.
